import styled from 'styled-components';
export const HeaderWrapper = styled.header`
  background: linear-gradient(90deg, rgb(255 255 255) 25%, rgb(214 228 228) 100%);
  width: 100%;
  position: sticky;
  top: 0;
  display: flex;
  flex-direction: row;
  height: 150px;
  align-items: center;
  justify-content: space-between;
  box-shadow: 1px 4px 10px rgba(141, 127, 127, 0.5);
  z-index: 1;
`;
